import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Postdetail = () => {
  return (
    <View>
      <Text>P</Text>
    </View>
  );
};

export default P;

const styles = StyleSheet.create({});
